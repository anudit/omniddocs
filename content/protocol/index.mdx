---
title: Overview
---

import Card, { Cards } from 'components/Cards';
import protocolDetails from 'components/protocolDetails.json'

<br/>
<Image src="/diagrams/arch-1-dark.png" alt="Home" width={500} height={500} className="themedImage"/>

<Cards
num={3}
children={Object.keys(protocolDetails).map((key, i) => (
<Card
    key={i}
    title={protocolDetails[key].title}
    description={protocolDetails[key].description}
    icon={protocolDetails[key].icon}
    href={protocolDetails[key].href}
/>
))}
/>
